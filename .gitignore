# Claude Code worktrees (temp directories, never commit)
.claude/worktrees/

# Node / build artifacts (if any)
node_modules/
docs/_app.jsx

# Playwright test outputs
test-results/
playwright-report/

# Local env (credentials — never commit)
.env.local
